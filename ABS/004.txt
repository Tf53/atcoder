list(map(sum,A))
Aの行を取り出して、行ごとにsum関数に放り込む

list(map(sum, zip(*A)))
*A: Aをバラバラにする[[1,2], [3,4]] -> ([1,2], [3,4])
zip: バラバラにされたリストから同じ列の要素をペアにする　行列の縦と横がひっくり返る

lambda j: str(yoko[i] + tate[j] - A[i][j])
range(W)からjを一つずつ受け取り、計算結果をstr()で文字列にしている

map(lambda, range(W))
jを0~W-1まで変化させながらループ

' '.join(...)
出来上がった文字列の集まりを' 'で連結